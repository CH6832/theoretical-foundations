### **Complex Numbers**
1. **Real and Imaginary Parts**: Express the complex number $( z = -3 + 4i )$ in terms of its real and imaginary parts.
2. **Polar Form**: Convert the complex number $( z = 3 - 4i )$ into polar form and express both the modulus and argument.
3. **Multiplication in Polar Form**: Multiply $( z_1 = 2e^{i\pi/6} )$ and $( z_2 = 3e^{i\pi/4} )$ using polar form.
4. **Euler’s Formula**: Using Euler's formula, express $( z = 5e^{i\pi/4} )$ in terms of trigonometric functions.
5. **Conjugation**: Given $( z = 7 - 6i )$, compute the complex conjugate $( \bar{z} )$ and verify that $( z \cdot \bar{z} )$ is a real number.
6. **Roots of Unity**: Find the 4th roots of unity and represent them graphically on the complex plane.
7. **Quadratic Equations**: Solve the quadratic equation $( z^2 + 4z + 13 = 0 )$ and express the solutions in polar form.
8. **Complex Powers**: Compute $( (1 + i)^5 )$ using polar form.
9. **De Moivre’s Theorem**: Use De Moivre’s theorem to compute $( (\cos \frac{\pi}{3} + i \sin \frac{\pi}{3})^6 )$.
10. **Applications in Circuit Analysis**: Explain how complex numbers are used to represent impedance in AC circuits, and calculate the total impedance of two components $( Z_1 = 4 + 3i )$ and $( Z_2 = 5 - 2i )$ connected in series.

---

### **Vectors**
11. **Dot Product**: Compute the dot product of $( \mathbf{u} = (3, -5) )$ and $( \mathbf{v} = (1, 2) )$, and interpret its meaning geometrically.
12. **Cross Product**: Find the cross product of $( \mathbf{u} = (1, 2, 3) )$ and $( \mathbf{v} = (4, -1, 2) )$.
13. **Angle Between Vectors**: Find the angle between the vectors $( \mathbf{u} = (1, 0, 1) )$ and $( \mathbf{v} = (0, 1, 1) )$.
14. **Projection**: Calculate the projection of $( \mathbf{u} = (2, 3) )$ onto $( \mathbf{v} = (1, 1) )$.
15. **Vector Decomposition**: Decompose the vector $( \mathbf{v} = (6, 8) )$ into components parallel and perpendicular to $( \mathbf{u} = (3, 4) )$.
16. **Triple Product**: Evaluate the scalar triple product for $( \mathbf{a} = (1, 2, 3) )$, $( \mathbf{b} = (4, -5, 6) )$, and $( \mathbf{c} = (-7, 8, -9) )$, and interpret the result geometrically.
17. **Area of a Parallelogram**: Find the area of the parallelogram spanned by the vectors $( \mathbf{u} = (2, 3) )$ and $( \mathbf{v} = (5, -1) )$.
18. **Vector Addition in Physics**: Given two forces $( \mathbf{F}_1 = (3, 4) )$ and $( \mathbf{F}_2 = (-2, 5) )$, compute the resultant force acting on a particle.
19. **Position and Velocity Vectors**: If a particle moves such that its position vector at time $( t )$ is $( \mathbf{r}(t) = (2t, 3t^2) )$, find its velocity and acceleration vectors.
20. **Applications of Vector Fields**: Describe how vector fields are used in fluid dynamics, and give an example of a simple vector field representing fluid flow.

---

### **Matrices and Linear Transformations**
21. **Matrix Multiplication**: Multiply the matrices $( A = \begin{pmatrix} 1 & 2 \\ 3 & 4 \end{pmatrix} )$ and $( B = \begin{pmatrix} 0 & 5 \\ 6 & 7 \end{pmatrix} )$.
22. **Matrix Determinant**: Calculate the determinant of $( A = \begin{pmatrix} 4 & 3 & 2 \\ 1 & 5 & 0 \\ 7 & 6 & 1 \end{pmatrix} )$.
23. **Rotation Matrix**: Derive the 2D rotation matrix for a counterclockwise rotation by $( 45^\circ )$ and apply it to the vector $( \mathbf{v} = \begin{pmatrix} 1 \\ 0 \end{pmatrix} )$.
24. **Shear Transformation**: Apply the shear matrix $( S = \begin{pmatrix} 1 & 2 \\ 0 & 1 \end{pmatrix} )$ to the vector $( \mathbf{v} = \begin{pmatrix} 3 \\ 1 \end{pmatrix} )$.
25. **Reflection Matrix**: Find the matrix that reflects a point across the line $( y = x )$, and apply it to the vector $( \mathbf{v} = \begin{pmatrix} 2 \\ 3 \end{pmatrix} )$.
26. **Composition of Transformations**: Given the transformation matrices $( A )$ and $( B )$, describe the geometric meaning of the product $( AB )$.
27. **Transformation of the Unit Circle**: Apply the matrix $( A = \begin{pmatrix} 2 & 0 \\ 0 & 1 \end{pmatrix} )$ to the unit circle, and describe the resulting ellipse.
28. **Matrix Equation**: Solve the matrix equation $( A \mathbf{x} = \mathbf{b} )$ where $( A = \begin{pmatrix} 2 & 1 \\ 1 & 2 \end{pmatrix} )$ and $( \mathbf{b} = \begin{pmatrix} 5 \\ 7 \end{pmatrix} )$.
29. **Applications in Computer Graphics**: Explain how transformation matrices are used in computer graphics to rotate, scale, and translate objects in 3D space.
30. **Symmetric Matrix**: Verify whether the matrix $( A = \begin{pmatrix} 1 & 2 & 3 \\ 2 & 4 & 5 \\ 3 & 5 & 6 \end{pmatrix} )$ is symmetric.

---

### **Vector Spaces**
31. **Basis and Dimension**: Find a basis and the dimension of the vector space spanned by the vectors $( (1, 0, 2) )$, $( (0, 1, 1) )$, and $( (2, 1, 3) )$.
32. **Null Space**: Find the null space of the matrix $( A = \begin{pmatrix} 1 & 2 & 3 \\ 4 & 5 & 6 \end{pmatrix} )$.
33. **Column Space**: Determine the column space of the matrix $( A = \begin{pmatrix} 2 & 3 \\ 4 & 6 \end{pmatrix} )$ and whether it spans $( \mathbb{R}^2 )$.
34. **Subspaces**: Determine if the set $( S = \{(x, y, z) \in \mathbb{R}^3 : x + y + z = 0 \} )$ is a subspace of $( \mathbb{R}^3 )$.
35. **Linear Dependence**: Show that the vectors $( \mathbf{v}_1 = (1, 2, 3) )$, $( \mathbf{v}_2 = (4, 5, 6) )$, and $( \mathbf{v}_3 = (7, 8, 9) )$ are linearly dependent.
36. **Gram-Schmidt Process**: Use the Gram-Schmidt process to orthogonalize the set of vectors $( \mathbf{v}_1 = (1, 1, 0) )$ and $( \mathbf{v}_2 = (1, 0, 1) )$.
37. **Inner Product Spaces**: Define an inner product on the space $( \mathbb{R}^3 )$, and use it to find the angle between the vectors $( (1, 0, 0) )$ and $( (0, 1, 1) )$.
38. **Isomorphisms**: Show that the vector space $( \mathbb{R}^2 )$ is isomorphic to the space of 2x1 column matrices.
39. **Row Space and Rank**: Find the rank of the matrix $( A = \begin{pmatrix} 1 & 2 & 3 \\ 0 & 1 & 4 \\ 5 & 6 & 0 \end{pmatrix} )$, and determine if the rows are linearly independent.
40. **Applications in Coding Theory**: Explain how vector spaces are used in error-correcting codes, and provide an example of a simple coding matrix.

---

### **Gaussian Elimination**
41. **Row Reduction**: Perform Gaussian elimination on the matrix $( \begin{pmatrix} 2 & 1 & 1 \\ 1 & 3 & 2 \\ 1 & -1 & 0 \end{pmatrix} )$.
42. **Rank via Gaussian Elimination**: Use Gaussian elimination to find the rank of the matrix $( A = \begin{pmatrix} 1 & 2 & 3 \\ 4 & 5 & 6 \\ 7 & 8 & 9 \end{pmatrix} )$.
43. **Solution of a 3x3 System**: Solve the system of equations $( x + y + z = 6 )$, $( 2x - y + z = 3 )$, and $( 3x + y - z = 4 )$ using Gaussian elimination.
44. **Infinite Solutions**: Determine whether the system of equations represented by the augmented matrix $( \left[ \begin{array}{ccc|c} 1 & 2 & 3 & 4 \\ 2 & 4 & 6 & 8 \\ 3 & 6 & 9 & 12 \end{array} \right] )$ has infinite solutions, and if so, describe the solution set.
45. **Back Substitution**: Solve the system $( x + 2y + 3z = 9 )$, $( 2y + 5z = 8 )$, $( z = 3 )$ using back substitution.
46. **Reduced Row Echelon Form**: Bring the matrix $( \begin{pmatrix} 1 & 2 & 3 \\ 0 & 1 & 4 \\ 0 & 0 & 1 \end{pmatrix} )$ to reduced row echelon form.
47. **Pivoting Strategy**: Explain the importance of pivoting in Gaussian elimination and demonstrate with an example.
48. **Applications in Economics**: Describe how Gaussian elimination can be used to solve input-output models in economics, and give a simple example with two industries.
49. **LU Decomposition**: Perform LU decomposition on the matrix $( A = \begin{pmatrix} 4 & 3 \\ 6 & 3 \end{pmatrix} )$, and verify the result by multiplying the factors.
50. **Applications in Robotics**: Explain how Gaussian elimination is used in robotics to solve systems of linear equations in motion planning.

---

### **Eigenvalues and Eigenvectors**
51. **Eigenvalue Calculation**: Find the eigenvalues of $( A = \begin{pmatrix} 2 & 1 \\ 1 & 2 \end{pmatrix} )$.
52. **Eigenvector Calculation**: Find the eigenvectors of $( A = \begin{pmatrix} 2 & 1 \\ 1 & 2 \end{pmatrix} )$, and verify your result.
53. **Diagonalization**: Diagonalize the matrix $( A = \begin{pmatrix} 4 & 1 \\ 2 & 3 \end{pmatrix} )$, if possible.
54. **Determinant from Eigenvalues**: Given that the eigenvalues of a matrix are $( \lambda_1 = 3 )$ and $( \lambda_2 = 2 )$, find the determinant of the matrix.
55. **Applications in Stability Analysis**: Describe how eigenvalues are used to analyze the stability of dynamical systems, and give an example in population modeling.
56. **Power Iteration**: Implement the power iteration method to approximate the dominant eigenvalue of the matrix $( A = \begin{pmatrix} 2 & 0 \\ 0 & 1 \end{pmatrix} )$.
57. **Geometric Multiplicity**: Find the geometric multiplicity of the eigenvalue $( \lambda = 2 )$ for the matrix $( A = \begin{pmatrix} 2 & 1 \\ 0 & 2 \end{pmatrix} )$.
58. **Characteristic Polynomial**: Compute the characteristic polynomial of $( A = \begin{pmatrix} 1 & 2 \\ 2 & 1 \end{pmatrix} )$ and use it to find the eigenvalues.
59. **Applications in Principal Component Analysis**: Explain the role of eigenvalues and eigenvectors in Principal Component Analysis (PCA), and outline the steps for performing PCA on a dataset.
60. **Symmetric Matrices and Eigenvalues**: Prove that the eigenvalues of a real symmetric matrix are always real.

---

### **Determinants**
61. **3x3 Determinant**: Calculate the determinant of $( A = \begin{pmatrix} 2 & 0 & 1 \\ 3 & -2 & 4 \\ 1 & 5 & -1 \end{pmatrix} )$.
62. **Cofactor Expansion**: Use cofactor expansion to find the determinant of $( A = \begin{pmatrix} 4 & 3 & 2 \\ 1 & 0 & 5 \\ 7 & 8 & 6 \end{pmatrix} )$.
63. **Determinants and Area**: Show that the determinant of a 2x2 matrix gives the area of the parallelogram spanned by its column vectors.
64. **Properties of Determinants**: Prove that the determinant of a product of two matrices is the product of their determinants, i.e., $( \det(AB) = \det(A) \cdot \det(B) )$.
65. **Row Operations and Determinants**: Explain how elementary row operations affect the determinant of a matrix.
66. **Determinants and Inverses**: Show that if a matrix has a zero determinant, it is not invertible, and provide an example.
67. **Laplace Expansion**: Use Laplace expansion along the second row to find the determinant of $( A = \begin{pmatrix} 3 & 0 & 2 \\ -1 & 4 & 0 \\ 2 & -3 & 1 \end{pmatrix} )$.
68. **Applications in Volume Calculation**: Explain how determinants are used to compute the volume of a parallelepiped in 3D space, and give an example.
69. **Determinants of Block Matrices**: Given $( A = \begin{pmatrix} B & 0 \\ 0 & C \end{pmatrix} )$, where $( B )$ and $( C )$ are square matrices, show that $( \det(A) = \det(B) \cdot \det(C) )$.
70. **Jacobian Determinants**: Explain the role of Jacobian determinants in changing variables for multiple integrals, and calculate the Jacobian for the transformation $( x = r \cos \theta )$, $( y = r \sin \theta )$.

---

### **Limits and Continuity**
71. **Limit of a Rational Function**: Compute $( \lim_{x \to 2} \frac{x^2 - 4}{x - 2} )$.
72. **L’Hopital’s Rule**: Use L’Hopital’s rule to evaluate $( \lim_{x \to 0} \frac{\sin x}{x} )$.
73. **Continuity of Piecewise Functions**: Determine if the function $( f(x) = \begin{cases} x^2 & \text{if } x \leq 1 \\ 2x - 1 & \text{if } x > 1 \end{cases} )$ is continuous at $( x = 1 )$.
74. **Squeeze Theorem**: Use the squeeze theorem to evaluate $( \lim_{x \to 0} x^2 \sin \frac{1}{x} )$.
75. **Continuity and Differentiability**: Explain why a function can be continuous but not differentiable, and provide an example.
76. **Limits Involving Infinity**: Compute $( \lim_{x \to \infty} \frac{2x^2 + 3x + 1}{x^2 - x + 4} )$.
77. **Continuity of Trigonometric Functions**: Prove that the sine and cosine functions are continuous for all $( x \in \mathbb{R} )$.
78. **One-Sided Limits**: Find the left-hand and right-hand limits of $( f(x) = \frac{|x|}{x} )$ as $( x \to 0 )$.
79. **Intermediate Value Theorem**: Use the intermediate value theorem to prove that the equation $( x^3 - 4x + 1 = 0 )$ has a solution in the interval $( (0, 2) )$.
80. **Applications in Economics**: Explain how limits and continuity are used in marginal analysis, and compute the marginal cost function given a total cost function $( C(x) = x^3 - 2x^2 + 5x + 10 )$.

---

### **Derivatives**
81. **Product and Quotient Rules**: Find the derivative of $( f(x) = \frac{x^3 - 2x}{x^2 + 1} )$.
82. **Chain Rule**: Differentiate the function $( f(x) = \sin(3x^2 + 2x) )$.
83. **Implicit Differentiation**: Use implicit differentiation to find $(\frac{dy}{dx} )$ if $( x^2 + y^2 = 25 )$.
84. **Tangent Line Approximation**: Find the equation of the tangent line to the curve $( y = x^3 - x )$ at $( x = 2 )$.
85. **Critical Points**: Find the critical points of the function $( f(x) = x^4 - 4x^3 + 10 )$, and classify them as maxima, minima, or saddle points.
86. **Applications in Physics**: Explain how derivatives are used to describe motion in physics, and find the velocity and acceleration of an object whose position is given by $( s(t) = t^3 - 3t^2 + 2t )$.
87. **Optimization Problems**: Solve the optimization problem: Find the dimensions of a rectangle with a perimeter of 24 units that has the maximum area.
88. **Second Derivative Test**: Use the second derivative test to determine whether $( f(x) = x^3 - 3x^2 + 4x )$ has a local minimum or maximum at $( x = 1 )$.
89. **Related Rates**: A balloon is being inflated and its volume is increasing at a rate of 5 cubic feet per minute. How fast is the radius of the balloon increasing when the radius is 2 feet?
90. **Logarithmic Differentiation**: Use logarithmic differentiation to find the derivative of $( f(x) = x^x )$.

---

### **Integration**
91. **Substitution Method**: Evaluate $( \int (3x^2 + 2x) e^{x^3 + x^2} \, dx )$ using substitution.
92. **Integration by Parts**: Use integration by parts to find $( \int x e^x \, dx )$.
93. **Improper Integrals**: Determine if the integral $( \int_1^\infty \frac{1}{x^2} \, dx )$ converges or diverges, and if it converges, find its value.
94. **Area Under a Curve**: Find the area under the curve $( y = x^2 )$ between $( x = 1 )$ and $( x = 3 )$.
95. **Applications in Economics**: Explain how integration is used to find consumer and producer surplus, and compute the consumer surplus given the demand curve $( p(x) = 100 - 2x )$ and a price of 40.
96. **Trigonometric Integrals**: Compute $( \int \sin^2 x \, dx )$.
97. **Volume of Revolution**: Find the volume of the solid obtained by rotating the curve $( y = x^2 )$ around the x-axis from $( x = 0 )$ to $( x = 2 )$.
98. **Partial Fraction Decomposition**: Evaluate $( \int \frac{2x + 1}{(x + 1)(x^2 + 1)} \, dx )$ using partial fraction decomposition.
99. **Numerical Integration**: Approximate the integral $( \int_0^1 e^{-x^2} \, dx )$ using the trapezoidal rule with 4 intervals.
100. **Applications in Probability**: Explain how integrals are used to find probabilities in continuous distributions, and find the probability that a standard normal variable lies between -1 and 1.
